import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(() => ({
  root: {
    margin: '0 auto',
    width: '20%',
    height: '5rem',
    textAlign: 'center',
  },
  ticketView: {
    marginTop: '30px'
  },
  btn:{
    marginTop: '20px'
  }
}));
